<% content_for :title, "Resend Unlock Instructions" %>

<div class="auth-shell">
  <section class="auth-card auth-card--narrow">
    <div class="auth-card__hero">
      <p class="auth-card__eyebrow">Account Access</p>
      <h1 class="auth-card__title">Resend unlock email</h1>
      <p class="auth-card__copy">
        Request a fresh unlock link if your account was locked and you need access again.
      </p>
    </div>

    <div class="auth-card__panel">
      <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "auth-form" }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="auth-form__field">
          <%= f.label :email %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "auth-form__input" %>
        </div>

        <%= f.submit "Resend unlock instructions", class: "market-button" %>
      <% end %>

      <%= render "devise/shared/links" %>
    </div>
  </section>
</div>
